services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: skudo
      POSTGRES_PASSWORD: skudo
      POSTGRES_DB: skudo_test
    ports: ["55432:5432"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U skudo"]
      interval: 2s
      timeout: 3s
      retries: 20
